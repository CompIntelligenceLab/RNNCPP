build/tests/test_diff_eq1/test_diff_eq1 -l 4 -npl 2 -nsl 4  -b 1 -s 2 -a "decayde" -r 1 -w "xavier" -i 1.e-2

#build/tests/test_diff_eq1/test_diff_eq1 -l 4 -nl 4  -b 1 -s 2 -a "decayde" -r 1 -w "xavier" -i 1.e-2

# -sl:  serial layers: standard configuration for deep networks: one layer follows the other towards the loss function. 
# -pl:  parallel layers: Input is connected to multiple layers, each one a deep network, and connected at the loss function. 

#build/tests/test_recurrent_model_bias1/test_recurrent_model_bias1 -l 1  -b 1 -s 4 -a "iden" -r 1 -w "xavier_iden" -i 1.e-4
# if seq_len = 1, -r should have no effect

#build/tests/test_recurrent_model_bias1/test_recurrent_model_bias1 -l 4  -b 1 -s 2 -a "iden" -r 1 -w "xavier"
